import { hot } from "react-hot-loader/root";
import React from "react";

class HelloMessage extends React.Component {
  render() {
    return <div>Petro</div>;
  }
}

export default hot(HelloMessage);
